<!-- ====== COMMENT FORM ====== -->
<div class="container">
  <form method="POST" action="/trucks/{{ truck.id}}" class="well">
    <div class="row">
      <div class="col-md-6">
        <div class="form-group">
          <label for="your-review">Your Review</label>
          <input type="text" class="form-control" id="review-body" value= "{{ comments.review}}" name="review-body">
        </div>
    <button type="submit" class="btn btn-default">Submit</button>
  </form>
</div>
